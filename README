Usage: rypper <uri> <selector>

Options:
    --akephalos, -a   uses akephalos-gem (HtmlUnit) for html-pages
    --harmony, -j     uses harmony-gem (envjs + johnson) for html-pages
    --help, -h        display help
    --output, -o      output directory
    --overwrite, -w   overwrite existing files
    --referer, -r     http referer for html request
    --selenium, -s    uses your firefox web-browser for html-pages
